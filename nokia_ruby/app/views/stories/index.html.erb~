<table>
<% @stories.each do |story| %>
<tr>
    <td><%= story.id %></td>
    <td><%= story.title %></td>
    <td><%= story.body %></td>
    <td><%= story.body %></td>
    <td><%= story.body %></td>
</tr>
<%end%>
</table>
