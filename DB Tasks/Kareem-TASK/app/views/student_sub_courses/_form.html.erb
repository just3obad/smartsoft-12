<%= form_for(@student_sub_course) do |f| %>
  <% if @student_sub_course.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@student_sub_course.errors.count, "error") %> prohibited this student_sub_course from being saved:</h2>

      <ul>
      <% @student_sub_course.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
